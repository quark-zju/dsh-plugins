# deepseek-harness plugins with customizations

To import a plugin:

```
sl subtree import --url https://github.com/USER/NAME.git --to-path NAME -r MAIN
```

All plugins are shipped source-ready. Installing should avoid extra build step.
